<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quotationapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuotationApp">

        <!-- LOGIN (LAUNCHER) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HOME -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- ADD COMPONENTS -->
        <activity
            android:name=".AddComponentsActivity"
            android:exported="false" />

        <!-- PREPARE QUOTATION -->
        <activity
            android:name=".PrepareQuotationActivity"
            android:exported="false" />

        <!-- PDF GENERATION -->
        <activity
            android:name=".QuotationPDFActivity"
            android:exported="false" />

        <!-- QUOTATION HISTORY -->
        <activity
            android:name=".QuotationHistoryActivity"
            android:exported="false" />

        <!-- PAYMENT QR -->
        <activity
            android:name=".PaymentQRActivity"
            android:exported="false" />

        <!-- COMPANY HEADER -->
        <activity
            android:name=".AddCompanyHeaderActivity"
            android:exported="false" />

        <!-- FILE PROVIDER -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
